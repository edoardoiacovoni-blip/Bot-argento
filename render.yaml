services:
  - type: worker
    name: bot-argento
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py
    envVars:
      - key: INTERACTIVE_MODE
        value: false
      - key: API_KEY
        sync: false
      - key: SECRET_KEY
        sync: false